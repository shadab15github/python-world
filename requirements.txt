# Intentionally OUTDATED pins for dependency-scanning / remediation testing.
# Oldest versions that still install cleanly on Python 3.12.
# Do NOT use this file as a template for real projects.

# --- packages the app uses directly ---
Flask==2.2.2            # CVE-2023-30861  (session cookie leak via caching proxy)
requests==2.25.1        # CVE-2023-32681  (Proxy-Authorization leak on redirect)
                        # CVE-2024-35195  (verify=False persists for the session)
arrow==1.2.3            # human-friendly dates (the "moment" of Python)
Jinja2==3.1.2           # CVE-2024-22195  (XSS via xmlattr filter)
                        # CVE-2024-34064  (xmlattr key injection)

# --- transitive deps, pinned old on purpose so they show up too ---
Werkzeug==2.2.2         # CVE-2023-25577, CVE-2023-46136, CVE-2024-34069
urllib3==1.26.5         # CVE-2021-33503, CVE-2023-43804, CVE-2023-45803
certifi==2020.12.5      # CVE-2022-23491, CVE-2023-37920
idna==2.10              # CVE-2024-3651   (ReDoS in idna.encode)
chardet==4.0.0
click==8.1.3
itsdangerous==2.1.2
MarkupSafe==2.1.5
python-dateutil==2.8.2
six==1.16.0
